name: C/C++ CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: CMake config
      run: |
        mkdir build
        cd build
        cmake ..
    - name: CMake build
      run: |
        pwd
        cmake --build .
